<h1>Listing posts</h1>

<table>
  <tr>
    <th>Favor title</th>
    <th>What this means to them</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= link_to post.title, post %></td>
    <td><%= post.meaning %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
